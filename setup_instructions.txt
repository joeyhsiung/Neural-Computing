1. Extract all the files from the zip file
2. Change directory (cd) to the extracted folder, and save stock_data.csv data into this root folder
3. Create a new virtualenv and install packages from requirements.txt:
virtualenv Student_env
cd Student_env
source bin/activate
cd -
pip install -r requirements.txt

4. Copy and paste the following code to open the IPYNB:
junru_xiong_submission.ipynb

5. If you want to load model to notebook, please follow below:
import pickle
filename = 'res_grid_svm.sav'
loaded_model = pickle.load(open(filename, 'rb'))